# K-Means Clustering on Income Dataset

This project performs **K-Means Clustering** on an income dataset using the features **Age** and **Income($)**.  
The goal is to group people into clusters based on similarities in age and income.

## 📂 Dataset
The dataset contains:
- Age  
- Income($)

## 🧪 Steps Performed
1. Load dataset using Pandas
2. Apply Min-Max Scaling to Age and Income
3. Run K-Means with 3 clusters
4. Add cluster labels to dataset
5. Visualize clusters using scatter plot with colors:
   - Cluster 0 → Red
   - Cluster 1 → Green
   - Cluster 2 → Blue

## 🛠️ Technologies Used
- Python  
- Pandas  
- Matplotlib  
- Scikit-Learn  

## ▶️ How to Run
1. Install dependencies:
 pip install pandas matplotlib scikit-learn

2. Run the script:
 python K-Means Clustering.py